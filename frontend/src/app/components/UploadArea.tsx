export default function UploadArea() {
  return (
    <div className="flex flex-col items-center justify-center">
      <button className="mt-4 bg-[var(--foreground)] text-[var(--background)] px-4 py-2 rounded hover:bg-[var(--foreground-hover)] transition-colors duration-300">
        Upload Your Paper Here
      </button>
    </div>
  );
}

